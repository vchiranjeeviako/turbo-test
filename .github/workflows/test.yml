on:
  push:
    
jobs:
  testing:
    name: Testing-workflow
    runs-on: ubuntu-20.04
    steps:
      - name: Checking out repo
        id: checkout-repo
        uses: actions/checkout@v3

      - name: Something
        if: always() && steps.checkout-repo.outcome == 'failure'
        run: |
          echo "came here"
        
      - name: Something else
        run: echo "should come"